// Dangerously gets a string from user using scanf with arry

#include <stdio.h>

int main(void)
{
        char s[4];
        printf("s: ");
        scanf("%s", s);
        printf("s is: %s\n", s);

        return 0;
}
