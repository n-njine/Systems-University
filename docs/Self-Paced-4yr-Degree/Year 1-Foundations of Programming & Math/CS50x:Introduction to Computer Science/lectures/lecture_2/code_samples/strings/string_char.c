// Treats string as array

#include <cs50.h>
#include <stdio.h>

int main(void)
{
        // Declare string "HI!"
        string s = "HI!";

        // Print string as an array of chars
        printf("char: %c%c%c\n", s[0], s[1], s[2]);

        // Exit successfully
        return 0;
}
